============
Installation
============

At the command line::

    $ easy_install django-auto-filter

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-auto-filter
    $ pip install django-auto-filter
